import BienvenidaScreen from './screens/BienvenidaScreen';
import { ThemeProvider } from './contexts/themeContext';


export default function App() {
  return (
    //Envolvemos la aplicacion en el ThemeProvider para que todos los componentes tengan acceso al tema
    //En este caso, la pantalla de entrada de nuestra app es BienvenidaScreen
    /*
    <ThemeProvider>
      <BienvenidaScreen />
    </ThemeProvider>
    */
   <></>
  );
}
